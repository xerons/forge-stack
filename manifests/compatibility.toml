# Known-good ranges; policy is warn-not-fail for untested newer versions unless a known
# incompatibility is documented here.

[agtx]
supported = ">=1.0.0,<2.0.0"
warn_only = true

[herdr]
supported = ">=0.8.0"
warn_only = true

[codex]
supported = ">=0.100.0"
warn_only = true

[rtk]
supported = ">=0.40.0"
warn_only = true
